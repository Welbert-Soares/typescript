namespace Geometria {
    export namespace Area {
        const PI = 3.1416;

        export function Circunferencia(raio: number): number {
            return PI * Math.pow(raio, 2);
        }

    }
}


